/* 
THIS PROJECT
*/

enum PheromoneType {
    TOHOME,
    TOFOOD,
}

/* 
OLD
*/
enum Mouse {
    LEFTDOWN = "0",
    LEFTUP = "1",
    RIGHTDOWN = "2",
    RIGHTUP = "3",
}

enum ActionTypes {
    MOUSE,
    KEYBOARD,
}

enum ButtonEnum {
    LEFT,
    MIDDLE,
    RIGHT,
}

export { Mouse, ActionTypes, ButtonEnum, PheromoneType };
